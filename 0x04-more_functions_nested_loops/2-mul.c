#include"main.h"

/**
* mul - return the multiplication
* @a: the character to be checked
* @b: the character to be checked
*
* Return: the multiplication of two digit
*/


int mul(int a, int b)
{
return (a * b);
}
