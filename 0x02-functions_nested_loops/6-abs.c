#include"main.h"

/**
 * _abs - return absolut methode of a intiger
 * @i: the character to be checked
 *
 * Return: Absolute value
 */

int _abs(i)
{
if (i >= 0)
{
return (i);
}
else
{
return (i * -1);
}
}
