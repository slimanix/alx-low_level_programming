#include"main.h"

/**
 * _abs - return absolut methode of a intiger
 * @int: the character to be checked
 *
 * Return: Absolute value
 */

int _abs(int)
{
if (int >= 0)
{
return (int);
}
else
{
return (int * -1);
}
}
