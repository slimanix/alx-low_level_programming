#include"main.h"

/**
 * _abs - return absolut methode of a intiger
 * @n: the character to be checked
 *
 * Return: Absolute value
 */

int _abs(int)
{
if (n >= 0)
{
return (n);
}
else
{
return (n * -1);
}
}
